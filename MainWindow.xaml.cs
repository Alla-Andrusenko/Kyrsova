using System.Windows;

namespace ParkingInterface
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
            // Видалено DataContext = new MainWindowViewModel();
        }
    }
}